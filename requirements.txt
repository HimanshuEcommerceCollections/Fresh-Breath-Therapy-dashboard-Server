fastapi==0.141.1
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.2.3
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
psycopg2-binary==2.9.9
email-validator==2.2.0
cloudinary==1.41.0
httpx==0.27.2
apscheduler==3.11.3
tzlocal==5.4.4
reportlab==4.2.5
openpyxl==3.1.5

# Transitive via fastapi, pinned explicitly because it carried 9 advisories
# at 0.38.6 and is the layer every request passes through.
starlette==1.6.0
# Transitive via python-jose[cryptography]; pinned for the same reason.
cryptography==50.0.0
